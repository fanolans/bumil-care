import 'package:flutter/material.dart';

import '../education_ui.dart';

class Edu extends StatelessWidget {
  const Edu({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: EducationPage()),
    );
  }
}
